{
  "name": "cogito",
  "version": "0.0.1",
  "description": "An empathetic, OS-layer tuning application for non-technical thinkers.",
  "bin": "out/cli.js",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/example/cogito"
  },
  "scripts": {
    "build": "esbuild src/cli.tsx --bundle --platform=node --format=esm --define:process.env.NODE_ENV='\"production\"' --banner:js='#!/usr/bin/env node\nimport { createRequire } from \"module\"; const require = createRequire(import.meta.url);' --outfile=out/cli.js && cp node_modules/yoga-wasm-web/dist/yoga.wasm out/ && chmod +x out/cli.js",
    "start": "npm run build && ./out/cli.js"
  },
  "dependencies": {
    "ink": "^4.4.1",
    "ink-text-input": "^5.0.1",
    "meow": "^13.0.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/react": "^18.2.48",
    "@typescript-eslint/eslint-plugin": "^6.18.1",
    "@typescript-eslint/parser": "^6.18.1",
    "esbuild": "^0.19.11",
    "eslint": "^8.56.0",
    "react-devtools-core": "^4.28.5",
    "typescript": "^5.3.3"
  }
}
